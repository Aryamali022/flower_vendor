{
  "name": "vijay-flower-center",
  "version": "1.0.0",
  "private": true,
  "description": "Vijay Flower Center — order management (Capacitor Android app)",
  "scripts": {
    "build:web": "node copy-web.js",
    "sync": "npm run build:web && cap sync",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor-community/speech-recognition": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
